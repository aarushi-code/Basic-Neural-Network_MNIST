# Basic-Neural-Network_MNIST
Training a basic neural network using MNIST Dataset
The above model has been coded from scratch to build a neural network using MNIST Dataset. It is advisable to try out this project for anyone wanting to start working with neural networks.
